{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --ignore-scripts && npm rebuild bcrypt better-sqlite3 && npm run build"
  },
  "deploy": {
    "startCommand": "npm start"
  }
}
