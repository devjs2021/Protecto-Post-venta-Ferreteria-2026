{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "DATABASE_URL=postgresql://dummy:d@localhost/d npm install && npm run build"
  },
  "deploy": {
    "startCommand": "node start.cjs"
  }
}
