{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "DATABASE_URL=postgresql://dummy:d@localhost/d npm ci && npm run build"
  },
  "deploy": {
    "startCommand": "npm start"
  }
}
